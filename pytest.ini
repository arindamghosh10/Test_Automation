[pytest]
addopts = -v --tb=short --alluredir=reports/allure
markers =
    smoke: Quick sanity checks - page loads and no errors
    auth: Authentication related tests
    functional: CRUD and business logic tests
    multi_user: Tests requiring multiple simultaneous sessions
    cleanup: Teardown/cleanup tests - run last
testpaths = tests
